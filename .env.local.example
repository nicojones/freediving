# Local development — copy to .env.local and fill in
# Next.js auto-loads .env.local (never commit .env.local)

# Required for AI voice mode (Phase 19)
GEMINI_API_KEY=your_api_key_from_google_ai_studio
